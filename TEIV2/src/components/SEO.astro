---
const { title = '', description = '' } = Astro.props;
---
<>
  <title>{title ? `${title} — TEI Electrical` : 'TEI Electrical'}</title>
  <meta name="description" content={description} />
  <meta name="robots" content="index, follow" />
</>
